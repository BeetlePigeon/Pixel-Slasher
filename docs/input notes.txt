1. Fix Shift behavior before refactor
   Shift+LMB on interactable -> attack_in_place
   Shift+RMB on interactable -> attack_in_place
   Shift+keyboard skill on interactable -> attack_in_place

2. Rename soft_skill_use_or_attack_air -> attack_in_place
   The order means:
     use skill without locomotion
     soft_targeting decides whether to acquire a soft target first

3. Make keyboard number skills use the same path as RMB
   Keyboard skill press should map through skill_button.normal or skill_button.shift.
   It should create/replace action orders just like RMB.

4. Discuss policy archetypes per top-level field
   Define exact behavior/options for:
     interactable
     enemy
     no_target
     soft_targeting

5. Introduce player_input_policy and archetype expansion
   Rename:
     targeting_policy -> player_input_policy

   New conceptual shape:
     left_click.normal
     left_click.shift
     skill_button.normal
     skill_button.shift

6. Migrate one melee skill and one ranged skill first
   Suggested:
     debug_slash
     frozen_orb

   Test all combinations:
     left_click normal
     left_click shift
     skill_button normal via RMB
     skill_button shift via Shift+RMB
     skill_button normal via keyboard
     skill_button shift via Shift+keyboard

   Test hover states:
     interactable
     enemy in range
     enemy out of range
     empty ground

7. Migrate remaining skills

8. Remove fallback/default policy getters
   Missing policy/context/field should fail loudly.
   Unknown archetype should fail loudly.
   Unknown mode/order should fail loudly.

9. Character movement speed refactor

10. Homing turn-rate resolution

11. Homing target-only contact filtering