{
  "id": "spiral_projectile",
  "name": "Spiral Projectile",
  "cooldown_ticks": 0,
  "trigger_mode": "held_repeat",

  "blocked_by_motion_tags": ["dash"],
  "blocked_by_action_tags": [
    "cast",
    "channel",
    "recovery",
    "stun",
    "skill_locked"
  ],
  "cancels_action_tags": [],

  "required_components": ["transform"],
  "required_params": [
    "projectile_id",
    "spawn_origin"
  ],

  "allowed_param_values": {
    "spawn_origin": ["caster"]
  },

  "aim": null,

  "cast": {
    "duration": 20,
    "tags": ["cast", "movement_locked", "skill_locked"],
    "events": [
      {
        "tick": 10,
        "handler": "execute_projectile",
        "params": {
          "spawn_angle_step_offset": 0
        }
      },
      {
        "tick": 10,
        "handler": "execute_projectile",
        "params": {
          "spawn_angle_step_offset": 32
        }
      },
      {
        "tick": 10,
        "handler": "execute_projectile",
        "params": {
          "spawn_angle_step_offset": 16
        }
      },
      {
        "tick": 10,
        "handler": "execute_projectile",
        "params": {
          "spawn_angle_step_offset": 48
        }
      }
    ]
  },

  "channel": null,

  "params": {
    "projectile_id": "spiral_projectile",
    "spawn_origin": "caster"
  },

  "handler": "execute_cast_skill"
}